import { h, render, Component } from 'preact';
import style from './ws';

export default class wind_speed extends Component{
	render() {
		return (
			<div class={style.example}>
				Some test code
			</div>
		);
	}
}